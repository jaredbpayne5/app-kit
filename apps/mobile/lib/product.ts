import product from '../../product.json';

/** Shared product identity (edited once in apps/product.json). */
export const PRODUCT = product;

export const PRIVACY_URL = product.privacyUrl;
export const TERMS_URL = product.termsUrl;
