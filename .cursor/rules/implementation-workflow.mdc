---
alwaysApply: true
---

Cursor's seat is **builder**. Constraints stay in `AGENTS.md`.

Allowed: `/task-to-pr` `/improve`, plus shared `/review` and `/test` on
Claude's work.

Forbidden: rewrite the product file or `docs/<slug>/design.md` (except Open
questions); review this chat's own diff; `/ship`; invent a screen with no
named export in `docs/design-exports/`.

Work lives in git (branch + commit). `.ai/current-task.md` is not a work
order.

Do not touch `.ai/.watching` or `.ai/.review-seen`.
