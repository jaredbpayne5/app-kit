---
alwaysApply: true
---

Cursor implements. Claude plans, writes mailbox tasks, and reviews.

Mailbox = `.ai/current-task.md`. "check your mail" / "check the mailbox" /
"what's in the mailbox?" mean that file.

- Owner `cursor` and Status `ready-for-cursor` → open the `mailbox` skill
  and follow it. Do not reconstruct pickup from memory.
- Owner `claude` → do not write source files.
- Propose; do not substitute. Full text is in the `mailbox` skill.
- Do not touch `.ai/.watching` or `.ai/.review-seen`.
